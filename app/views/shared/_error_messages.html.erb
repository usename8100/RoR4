<% if @user.errors[:name].any? %>
	<div class="error_explanation">
		<div class="alert alert-danger">
			<ul>
			<% @user.errors.each do |msg| %>
				<li><%= msg.full_message(:name, 'is invalid') %></li>
			<% end %>
			</ul>
			
		</div>
		
	</div>
<% end %>